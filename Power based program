#include <stdio.h>

int main()
{
    int base, power, i;
    int result = 1;

    printf("Enter base and exponent: ");
    scanf("%d %d", &base, &power);

    for(i = 1; i <= power; i++)
    {
        result = result * base;
    }

    printf("Result = %d", result);

    return 0;
}
